<nav class="navbar navbar-light bg-light static-top">
  <div class="container">
    <div class="d-flex justify-content-between align-items-center" style="width: 100%;" >
      <a class="navbar-brand" href="#!"><%= image_tag("logo-client-blk.png", style: "width: 200px;") %>  </a>
      <% if user_signed_in? %>
        <% if current_user.businesses.present? %>
          <%= link_to "Business Goals", business_path(current_user.businesses.first), class: "btn btn-success" %>
        <% end %>
      <% else %>
        <%= link_to "Log In", new_user_session_path, class: "btn btn-success" %>
      <% end %> 
    </div>
  </div>
</nav>